# Production Environment Variables
# Copy this file to .env.prod and fill in your values
# Usage: export $(cat .env.prod | xargs)

# Application - IMPORTANT: Generate a secure random string!
# Generate with: openssl rand -hex 32
APP_SECRET=changeme_generate_a_secure_random_string_min_32_chars

# Server configuration for HTTPS automatic (Let's Encrypt)
SERVER_NAME=yourdomain.com
# For multiple domains:
# SERVER_NAME=yourdomain.com,www.yourdomain.com

# MySQL Database Configuration (recommended for production)
MYSQL_ROOT_PASSWORD=your_secure_root_password_change_me
MYSQL_PASSWORD=your_secure_user_password_change_me
DATABASE_URL=mysql://ia_user:your_secure_user_password_change_me@mysql:3306/ia_challenge?serverVersion=8.0&charset=utf8mb4

# Alternative: PostgreSQL (if you prefer)
# DATABASE_URL=postgresql://user:password@database:5432/ia_challenge?serverVersion=16&charset=utf8

# Alternative: SQLite (not recommended for production)
# DATABASE_URL=sqlite:///%kernel.project_dir%/var/data.db

# OpenAI API Key (optional - only if not using Puter.js for image generation)
# The app uses Puter.js by default in frontend
OPENAI_API_KEY=sk-your-openai-api-key-here

# Frontend API URL - Relative path (same domain as frontend)
# Laisser /api car le frontend et le backend sont sur le même serveur
VITE_API_URL=/api

# Mercure (Real-time updates) - Generate a secure key!
# Generate with: openssl rand -base64 32
MERCURE_JWT_KEY=p1KoDtEqxReAuVphusSuYJk2nZ0tOmVs/aVHpp+t8RY=

# FrankenPHP Configuration (Performance tuning)
FRANKENPHP_NUM_THREADS=auto

# CORS Configuration (automatically set from SERVER_NAME)
# Only modify if you need custom CORS rules
